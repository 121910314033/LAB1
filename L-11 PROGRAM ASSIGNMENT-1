#Create a list with the user input data. Implement insertion sort (Recursive). 

def x(a,b): 
	
	if b<=1: 
		return	
	x(a,b-1) 
	last = a[b-1] 
	j = b-2	
	while (j>=0 and a[j]>last): 
		a[j+1] = a[j] 
		j = j-1

	a[j+1]=last

def y(a,b): 
	for i in range(b): 
		print (a[i]), 
a = [33,4,60] 
b = len(a) 
x(a, b)
print("The elementing after sorting is:")
y(a, b) 
OUTPUT:
The elementing after sorting is:
4
33
60
